"""Hugging Face Spaces Streamlit entry point.

The Docker Streamlit template expects this filename. The actual app lives in
app.py so local development and deployment use the same code path.
"""

from app import main


main()
